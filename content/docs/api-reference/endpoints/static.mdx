---
title: Static Media Files
description: Static Media Files endpoints reference
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { Callout } from "fumadocs-ui/components/callout";

## Get Agent Media File

Serve uploaded agent media files

### Endpoint

```
GET /media/uploads/{agentId}/filename.jpg
```

### Parameters

| Name | Type | In | Required | Description |
|------|------|-----|----------|-------------|
| agentId | string | path | Yes | - |

### Response

**Status Code:** 200 OK

Successful response

### Example

<Tabs items={["cURL", "TypeScript", "Python"]}>
  <Tab value="cURL">
    ```bash
    curl -X GET \
      -H "apikey: your-api-key" \
      http://localhost:3000/media/uploads/{agentId}/filename.jpg
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    const response = await fetch('http://localhost:3000/media/uploads/{agentId}/filename.jpg', {
      method: 'GET',
      headers: {
        'apikey': 'your-api-key',
      },
    });
    const data = await response.json();
    ```
  </Tab>
  <Tab value="Python">
    ```python
    import requests

    response = requests.get(
        'http://localhost:3000/media/uploads/{agentId}/filename.jpg',
        headers={'apikey': 'your-api-key'},
    )
    data = response.json()
    ```
  </Tab>
</Tabs>

---

## Get Generated Agent File

Serve generated agent files

### Endpoint

```
GET /media/generated/{agentId}/filename.jpg
```

### Parameters

| Name | Type | In | Required | Description |
|------|------|-----|----------|-------------|
| agentId | string | path | Yes | - |

### Response

**Status Code:** 200 OK

Successful response

### Example

<Tabs items={["cURL", "TypeScript", "Python"]}>
  <Tab value="cURL">
    ```bash
    curl -X GET \
      -H "apikey: your-api-key" \
      http://localhost:3000/media/generated/{agentId}/filename.jpg
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    const response = await fetch('http://localhost:3000/media/generated/{agentId}/filename.jpg', {
      method: 'GET',
      headers: {
        'apikey': 'your-api-key',
      },
    });
    const data = await response.json();
    ```
  </Tab>
  <Tab value="Python">
    ```python
    import requests

    response = requests.get(
        'http://localhost:3000/media/generated/{agentId}/filename.jpg',
        headers={'apikey': 'your-api-key'},
    )
    data = response.json()
    ```
  </Tab>
</Tabs>

---

## Get Channel Media File

Serve uploaded channel media files

### Endpoint

```
GET /media/uploads/channels/{channelId}/filename.jpg
```

### Parameters

| Name | Type | In | Required | Description |
|------|------|-----|----------|-------------|
| channelId | string | path | Yes | - |

### Response

**Status Code:** 200 OK

Successful response

### Example

<Tabs items={["cURL", "TypeScript", "Python"]}>
  <Tab value="cURL">
    ```bash
    curl -X GET \
      -H "apikey: your-api-key" \
      http://localhost:3000/media/uploads/channels/{channelId}/filename.jpg
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    const response = await fetch('http://localhost:3000/media/uploads/channels/{channelId}/filename.jpg', {
      method: 'GET',
      headers: {
        'apikey': 'your-api-key',
      },
    });
    const data = await response.json();
    ```
  </Tab>
  <Tab value="Python">
    ```python
    import requests

    response = requests.get(
        'http://localhost:3000/media/uploads/channels/{channelId}/filename.jpg',
        headers={'apikey': 'your-api-key'},
    )
    data = response.json()
    ```
  </Tab>
</Tabs>

---

